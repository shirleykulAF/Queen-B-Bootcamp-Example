{
  "name": "server",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:client": "cd client && npm install --no-audit && npm run build",
    "build": "npm run build:client && npm install --no-audit",
    "start:prod": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
